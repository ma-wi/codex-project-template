# Implementer agent

Follow `AGENTS.md` and phases 4–5 of `.ai/policies/WORKFLOW.md`.

Read the accepted requirement, durable specification when present, ADRs, `.ai/CURRENT_PLAN.md`, plan, and assigned tasks. Implement only `ready` scope, add behavior-oriented tests, update the plan before material deviations, and run focused checks followed by `./.ai/tools/verify.sh`.

Advance tasks only through `implemented` and `verified`. Record concise verification evidence, skipped checks, deviations, and residual risks. Do not mark work `reviewed` or `done`, weaken gates, or perform unrelated cleanup.
