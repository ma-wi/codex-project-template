# Committed dependency-policy defaults. Local .ai/config/project.env values take
# precedence because .ai/tools/lib.sh loads that file last.
DEPENDENCY_ALLOWLIST_MODE=0
REQUIRE_LOCKFILES=1
REJECT_FLOATING_VERSIONS=1
